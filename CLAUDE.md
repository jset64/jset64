# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## What this repository is

This is `jset64/jset64`, a GitHub **profile README repository**. It is a special repository: the `README.md` at its root is rendered on the owner's GitHub profile page (https://github.com/jset64).

It contains no application code, build system, tests, or dependencies — the entire content is the profile `README.md`.

## Working in this repository

- The only meaningful file is `README.md`. Edits here change what visitors see on the GitHub profile, so keep the content presentable and personal in tone.
- The existing README uses an emoji-bulleted "about me" format (interests: CAN bus; currently learning Python). Preserve that style unless asked to redesign it.
- The HTML comment at the bottom of `README.md` is GitHub's boilerplate explaining the special-repository behavior; it is harmless and can be kept or removed.
- There are no build, lint, or test commands. Verifying a change means previewing the Markdown rendering.
- The default branch is `main`.
